# Base URL for the Vanrang Foundation API (see the shared API contract doc).
# Copy this file to .env for local development and .env.production for the
# production build.
REACT_APP_API_BASE_URL=http://localhost:8080/api/v1
